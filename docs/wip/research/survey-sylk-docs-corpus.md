# Sylk VFS docs-corpus survey (helper-agent output, verbatim)

Raw report of a helper agent that read the VFS-relevant documents under
`/Users/adalundhe/Projects/sylk/docs/` (PARALLEL_GLOBAL_VFS.md, TOOL_VFS.md, ARCHITECTURE.md VFS
regions, SYSTEM.md, SYSTEM_IMPROVEMENTS.md, SINGLE_PANE.md, CLUSTER.md, CONFIRMATIONS.md,
PERFORMANCE.md, WAL_TODO.md, DB.md, DB_TODO.md, TODO.md, CLAIMS*.md, FABRIC.md, MCP.md,
PIPELINE_SKILL_REFACTOR.md, RESOLVER.md, docs/bugs/*, docs/fixes/*). Kept verbatim as evidence for
`survey-sylk-vfs.md`. Paths are relative to `/Users/adalundhe/Projects/sylk/`; quoted phrases are
verbatim.

## Scope notes and non-VFS files

- `docs/CHUNKING.md` is about semantic text chunking for embeddings (tree-sitter AST chunking into hierarchical levels, `ChunkStore` as SQLite persistence at CHUNKING.md:800, :945-967, :2078), not the VFS chunk arena. Not VFS-relevant beyond the name collision "ChunkStore".
- `docs/OPTIMIZATION.md` is a vector-quantization proposal (RaBitQ/LOPQ/adaptive codebooks). Not VFS.
- `docs/fixes/06-26-2026_EXTRANEOUS.md` and `docs/fixes/test.md` contain no VFS content.
- `docs/ARCHITECTURE.md:20784` says "See `FILESYSTEM.md` for complete specification" — that file does not exist in `docs/`.
- Two distinct in-repo VFS families are documented: `core/versioning` (SessionVFS/PipelineVFS/ReplicaVFS/MergePipe/CommitQueue/CommitResolver/ControlWAL; PARALLEL_GLOBAL_VFS.md:4) and `core/purevfs` ("per-pipeline + global in-memory VFS for agent-authored content" TOOL_VFS.md:9; `purevfs.ExecutionBroker.Run` FABRIC.md:741; `core/purevfs/chunk_arena_*.go` SYSTEM_IMPROVEMENTS.md:61). A third, the Tool VFS "substrate" (`core/substrate`, "84 files, in-flight" SINGLE_PANE.md:81-82), is a separate content-addressed store projected via FUSE.

## A. VFS LAYERS

**Replica chain / SessionVFS (core/versioning) — PARALLEL_GLOBAL_VFS.md (PGV)**
- Status: "Canonical specification, revised against the landed implementation (Stages 1–5 complete)" PGV:3. Scope `core/versioning`, `agents/shared`, global inspector/tester, architect PGV:4. The orchestrator "has no role" PGV:9-45.
- Chain: "Every pipeline merge into the global state produces a new `ReplicaVFS_N` — a full in-RAM VFS scoped to merge N, with a parent pointer to `ReplicaVFS_{N-1}`" PGV:91. Diagram PGV:93-105: "Disk state (authoritative, committed merges only, updated by the commit resolver)" at the bottom with "read-through for uncached paths"; `ReplicaVFS_1 ... parent = nil (direct read-through to disk)`.
- ReplicaVFS contents PGV:210-219: "built on the `PipelineVFS` foundation"; local content store "(writes + read-cache from ancestors)"; local `deletes` set; "An exact owned-path set; ownership checks during chain walks are the per-layer map lookups themselves (exact, no false positives — strictly better than the bloom filter originally sketched here)"; a JIT LRU read cache whose capacity is "(this merge's PathCount + every ancestor's, snapshotted at construction) × a per-path context fanout (the file, its imports, its test, its callers). No hand-picked entry counts; overflow evicts the coldest entry, never the whole cache" PGV:215; a mutex; parent pointer or nil; sealed flag; "A retention reference on its parent (preventing parent GC)". (§6.3 PGV:464-474 still says "Bloom filter over owned paths" — superseded by §3.6.)
- Memory: "**Memory cost per replica** = paths it wrote + paths it read-cached (LRU-bounded as above). Unread paths are pointer references; large codebases keep most files on disk and pay the read cost only where replicas look" PGV:221.
- Reads PGV:132-135: own store → parent chain → "read from disk. Disk is the authoritative committed state ... Optionally cache the resolved content into `ReplicaVFS_N`".
- Writes PGV:137-141: serialize on the mutex; apply in-RAM; "Append `ControlKindReplicaOverlayWrite` to the session's ControlWAL with writer-ID + role"; "**Never touches disk.** Only the commit resolver writes to disk." Delete appends `ControlKindReplicaOverlayDelete` PGV:472. Inspector and tester share one ReplicaVFS; "Last-write-wins in the shared overlay; WAL attribution makes it auditable" PGV:143.
- CoW trigger: per-file, on touch — "COW per-file (§3.6) materializes only what a given audit actually touches" PGV:81; "Sharing between inspector and tester: both operate on the same `ReplicaVFS_N`" PGV:225.
- Seal/abandon PGV:236-256: Seal freezes the overlay, computes diff vs parent chain, submits to MergePipe as `audit_addendum=true`; coordinator "skips audit dispatch for this entry and mark it accepted directly" PGV:242. "Seal-marker durability is advisory. `Seal()` transitions in memory first ... then appends the durable seal marker. A marker-append failure returns the diff alongside `ErrControlMarkerNotDurable`; the coordinator logs it and STILL submits the addendum" PGV:252. "`Abandon()` follows the same in-memory-first discipline: chain bypass for rejected work must take effect even when the disk is failing" PGV:254.
- Rejection/chain surgery PGV:258-280: rejected `ReplicaVFS_K` marked abandoned; "its effective parent becomes K's parent" PGV:276; supersedor M "is constructed with parent = K's parent (bypass)" PGV:278; remediation pipelines open with `BeginPipelineConfig.BaseCopyVersion = K` ("byte-for-byte materialization from the rejected merge's Copy") PGV:266.
- Copies: "Copies remain reconstructable from the semantic WAL, so remediation dispatch targeting a released version still materializes byte-for-byte; `CopyAt` fails loudly only for genuinely invalid versions" PGV:398. Stage 2 adds `CopyAt`, `MaterializePipelineFromCopy`, `MergeDescriptor`, arrival_seq PGV:592. CLAIMS.md:2041 "`MergePipelineIntoGreen` merges the pipeline's VFS into green, producing Copy_N at arrival_seq N"; CLAIMS.md:2050 "Copy_N materializes (byte-for-byte VFS replica)".
- Merge/commit flow PGV:23-45 (all direct calls; only `AuditMergeResultTopic` is a bus hop, post hoc). `MergePipelineIntoGreen` PGV:113-118: MergePipe OT vs current green → `MergeDescriptor` with `PipelineInspectorCertificate` → enqueue (ControlWAL write-ahead) → synchronous merge callbacks. Descriptor/entry fields PGV:169-188 (`PipelineID, BaseVersion, MergedVersion, Paths/PathCount, MergedAt, PipelineCertificate, AuditAddendum, SupersedesVersion`; `CommitEntry.State ∈ auditing|accepted|rejected|superseded|committed|abandoned`). Full lifecycles PGV:294-383.
- SessionVFS owns PGV:428-450: MergePipe; "Semantic WAL (disk-backed `VersionedWAL`; records merge descriptors)"; "Control WAL (disk-backed append-only log ...)"; CommitQueue; CopyRetention; ReplicaLifecycleLog; CommitResolver ("started in `Open`, stopped in `Close`"); merge-callback registry; `BeginAuditReplicaVFS(mergedVersion)`; on-disk `semantic-wal/` and `control-wal/log.bin` PGV:437-439.
- Cleanup PGV:386-398: releasable when terminally resolved, no child holds it, no pending remediation targets its Copy; "Every hold is an EXPLICIT, WAL-backed retention reference ... a child ReplicaVFS acquires a `replica-chain:<child-version>` hold on its parent's Copy"; GC "runs to FIXPOINT" PGV:394; released overlays are "unreconstructable; lookups return nil and consumers nil-skip" PGV:398.
- **Disk-touching paths in this layer (all documented):** (1) `CommitResolver.flushToDisk` / `DiskFlusher.Flush(diff)` PGV:39, :364-366, :461; (2) semantic WAL files `semantic-wal/` PGV:438; (3) ControlWAL `control-wal/log.bin` PGV:439, compaction "temp file → fsync → atomic rename → dir sync" PGV:544; (4) session close "Semantic WAL fsync + close; ControlWAL fsync + close (SessionCloseEpoch marker)" PGV:379-381; (5) chain-miss read-through to disk PGV:134; (6) optional forensic archive "serialized to a forensic directory with TTL retention" PGV:417-421. SINGLE_PANE.md:97 "Today 'disk' means the local filesystem the resolver flushes to."

**PipelineVFS / CVS / VirtualFilesystem (ARCHITECTURE.md, SYSTEM.md, TODO.md)**
- Dual VFS: `preChangeVFS` "Frozen snapshot at task start", `changeVFS` "Working copy" ARCHITECTURE.md:20996-20998; API `Read/ReadAt/Write/Delete/History/Diff/Commit/Rollback/ForkVariant/ChangedFiles` ARCHITECTURE.md:21024-21052. SYSTEM.md:585-586: "Read path: changeVFS → fallthrough to preChangeVFS → fallthrough to physical FS / Write path: Always to changeVFS (physical FS never written directly)". K8s mapping SYSTEM.md:550-555.
- CVS struct ARCHITECTURE.md:20801-20820: per-file version DAG, append-only `OperationLog`, content-addressed `BlobStore`, `WAL`, `VectorClock`, `mu sync.RWMutex`, per-file `fileLocks`. `FileVersion` (SHA-256 `VersionID`, multiple `Parents`, `ContentHash`, `VariantGroupID`) ARCHITECTURE.md:20823-20839; `Operation` with AST `Target` ARCHITECTURE.md:20842-20885; OT engine/conflict types ARCHITECTURE.md:20916-20968.
- Legacy sandbox `VirtualFilesystem`: `PrepareForCommand` does `os.MkdirTemp(vfs.stagingDir, "cmd-")` and sets `SYLK_VFS_STAGING` ARCHITECTURE.md:20709-20720; `MergeChanges` walks the staging dir and copies into `workingDir` (disk) ARCHITECTURE.md:20757-20779. Config: `StagingRoot ~/.sylk/staging`, `CVSStoragePath ~/.sylk/versions`, `WALPath ~/.sylk/wal` ARCHITECTURE.md:15451-15465.
- Variants: staging `/tmp/sylk/variants/{groupID}/original` and `vfs.Seed(path, currentDiskContent)` ARCHITECTURE.md:40960-40962; `CommitVariant` uses `os.WriteFile` into the working dir ARCHITECTURE.md:41544-41562; gotchas: "Subprocesses bypass VFS → VFS intercepts subprocess writes via LD_PRELOAD/DYLD_INSERT_LIBRARIES" ARCHITECTURE.md:41142.
- TODO.md: Staging Manager spec `~/.sylk/staging/<pipeline-id>/`, copy on first write, hash-based conflict, atomic temp+rename merge, 24h max age TODO.md:11301-11342.

**PureVFS (core/purevfs) — SYSTEM_IMPROVEMENTS.md (SI), SINGLE_PANE.md, MCP.md**
- Hard constraint: "**PureVFS stays in RAM.** No spill-to-disk, no on-disk snapshots, no fsync. Memory pressure is handled by compression, dedup, eviction of reconstructible state, snapshot pruning" SI:10-12; "HAMT lineage, cross-pod content addressing" SI:13-14; "Today HAMT structural sharing keeps every snapshot alive forever if anyone held a reference" SI:53-54; "The arena abstraction already exists in `core/purevfs/chunk_arena_*.go`" SI:61; "Today's single mutex is the throughput ceiling" (ChunkStore) SI:63-64; "The VFS journal already exists" SI:164; `VolumeManager` in `core/container/pod/volume_manager.go` SI:132; "Two pipeline pods working on the same repo currently dedup at the store level but each pod's `VolumeManager` re-injects file access independently" SI:72-74. Kept as-is: "PureVFS in RAM with no spill; the OT serializer" SINGLE_PANE.md:323.

**Tool VFS substrate — TOOL_VFS.md (TV)**
- Primitives: Blob (BLAKE3, immutable, refcounted, Hot/Warm/Cold tiers) TV:128-134; Manifest TV:136-152; Recipe TV:154-172; Sandbox TV:174-186; View (`mount_root: /sylk/views/{id}/`, `layout: map[Path]BlobHash`) TV:188-199. "Strict no-disk for substrate content. Substrate bytes never persist to host disk" TV:14. Tiered cache TV:781-812. Lockfile "persisted in the substrate WAL (not on disk)" TV:756; "Extend existing purevfs WAL infrastructure for substrate WAL (don't add WAL #5)" TV:1019. Disk-touching: one mount-point directory per platform TV:14, :995-1011; `disk://` fallback reads host binaries TV:334-344.

## B. MOUNTING / PROCESS BROKER

- Projection layer: "read-only: composes pipeline ⊕ global ⊕ disk ⊕ substrate views; writable: per-build scratch projections (in-memory, captured to substrate on success, discarded on failure); backends: hanwen/go-fuse (Linux), cgofuse (macOS via macFUSE/FUSE-T, Windows via WinFsp)" TV:56-62.
- Migration: "Migrate `process_broker_linux.go` from `bazil.org/fuse` to `hanwen/go-fuse/v2/fs`; Side-by-side via build tag (`substrate_fuse_v2`) for one release; ... bazil dropped one release later; macOS/Windows continue with cgofuse" TV:1040-1046. Risk: "Hanwen FUSE migration regresses an edge case bazil quietly handled" TV:1170; "Confirm `mode & 0111` round-trips through projection on each platform" TV:1169; "FUSE projection synthesizes ACLs on Windows; refuse setuid/setgid bits at provisioning time" TV:1168.
- Mount points: `/sylk/` (Linux/macOS), `S:\sylk\` (Windows) TV:14; views at `/sylk/views/{view_id}/` TV:193. Bootstrapping TV:995-1011: Linux "`unshare(CLONE_NEWNS)` first, `mkdir -p /sylk/views/pipe-7` second — the directory only exists in the namespace, never on host disk. **Zero host-disk presence.**"; macOS/Windows "One empty, content-free root directory at agent install time, not per-pipeline. Flagged for explicit user sign-off." "This is the only host-disk concession in the entire design."
- Per-OS canonical-path projection (Layer 4) TV:686-707: Linux private mount namespace, `mount --make-rprivate /`, FUSE-mount view, read-only bind mounts; macOS "no mount namespaces" → shebang rewriting at extraction plus `DYLD_INSERT_LIBRARIES` interposer that "fails gracefully on hardened-runtime/notarized binaries"; Windows WinFsp drive letter, `DefineDosDevice` with `DDD_NO_BROADCAST_SYSTEM`, `IKnownFolderManager` redirection.
- Writable build projection TV:600-632: `/sylk/build`, `/sylk/output`, `/sylk/tmp`, `/sylk/home` are "RAM-backed FUSE state inside our Go process"; substrate writes return `EROFS`.
- Sandbox backends TV:451-540 (Linux namespaces + seccomp-bpf + cgroups v2; macOS sandbox-exec + Endpoint Security + Network Extension/pf; Windows AppContainer + Job Object + WFP + restricted token).
- Execution backend: "Resolve required tools from pipeline lockfile → Compose View → Spawn under platform sandbox + FUSE projection → Capture stdout/stderr/exit" TV:33-36. Strict-disk: "`purevfs.ExecutionBroker.Run` — Every command execution under strict-disk" FABRIC.md:741.
- Lifetime/cleanup: witnesses "expire when their pipeline terminates. When all witnesses for a manifest expire, refcount drops, substrate GC reclaims" TV:770; "Shutdown detaches mounts and records interrupted/shutdown artifacts" CLAIMS_AND_INFRASTRUCTURE.md:4618; "user cancels during merge; child VFS claims mark interrupted and no orphan mount remains" :4637-4638.
- Legacy OS sandbox (ARCHITECTURE.md): "Sandboxing is OFF by default" :20440; bubblewrap args :20575-20600; Seatbelt profile written to `os.TempDir()` :20626; `FallbackOnError` runs unsandboxed with warning :20495; Windows Job Objects are stubs TODO.md:13875-13877.
- Placement: "A session's VFS plane (SessionVFS, MergePipe, resolver, ChunkStore, replica chain) is a **colocation unit**: it lives wholly on one node, and moves only via suspend/export/resume" SINGLE_PANE.md:91-93.

## C. WRITE SESSIONS / WAL / COMMIT QUEUE / DISK FLUSHER

- ControlWAL discipline: "every transition (`Enqueue`, `MarkAccepted`, `MarkRejected`, `MarkSuperseded`, `MarkCommitted`, `Abandon`) is write-ahead to the ControlWAL before the in-memory state mutates. Crash recovery: replay the WAL, reconstruct the queue, restart the resolver" PGV:194-196. "In-flight ReplicaVFS writes are durable via the ControlWAL; a crash mid-write replays on next open" PGV:529.
- Resolver: "a single goroutine woken by commit-queue events (`CommitQueue.Subscribe`) with a ticker as the fallback heartbeat" PGV:186; head `accepted` → flush, `committed`, pop, advance water line, GC; `auditing` → wait; `rejected` without supersedor → block; with supersedor → flush supersedor in its slot PGV:188-193. Subscription "Delivery is non-blocking: a slow subscriber's events drop and it recovers via `Snapshot()`" PGV:198.
- Ordering: "MergePipe OT linearizes all merges ... Commit queue enforces FIFO arrival order on disk commits. Disk only receives accepted-and-unblocked changesets" PGV:521.
- Flush confirmation gate PGV:200-208: Approve / Always flush / Hold; "Headless runs and tests have no confirmer registered and flush directly — an explicit boot-time policy, not a fallback". CONFIRMATIONS.md:110-118: `vfs_flush` "Gate errors hold; they never authorize."
- Never-teardown rule: "rejection/correction are never terminal for the VFS" CONFIRMATIONS.md:116.
- Crash behavior: "Durable queue ... Restart replays the WAL; in-flight replicas re-launch with deterministic IDs" PGV:507; "**Crash vs clean close — identical recovery path**: the WAL is the single source of truth" PGV:515.
- ControlWAL compaction PGV:544: rewrite "when obsolete entries OUTNUMBER live ones — a majority rule ... no size thresholds"; crash-safe rewrite; seq high-water marker; predicate "defaults to KEEP".
- Backpressure: "ON BY DEFAULT with a host-derived ceiling — `DeriveDispatchMaxInFlight()` = GOMAXPROCS × auditReplicasPerMerge(2) ... a negative value (and only a negative value) disables the gate" PGV:407.
- Legacy session WAL (ARCHITECTURE.md): `~/.sylk/sessions/{id}/wal/`, "fsync after each write" :6812-6817; checkpoints every 5 s, keep last 3 :6821-6837; "Maximum data loss: 5 seconds of work" :6852. `concurrency.WALConfig` sync modes, double buffering PERFORMANCE.md:389-410. `WALBudget` 200MB ARCHITECTURE.md:12659.
- Knowledge-graph CommitWAL bug `docs/bugs/commit-wal-segment-accumulation.md`: root causes :8-10 (segment-per-boot minting; `Checkpoint()` had "zero production callers"; recovery cleaned only after crashes), latent bug :14 (`gcOneSegment` could remove the open segment), fixes :26-30. WAL_TODO.md: seven WAL surfaces :10-18; commit atomicity "`OpCommitBegin`/`OpCommitEnd` bracket; absent end = incomplete = discard on recovery" :147; adaptive checkpoint trigger :83-87.
- Future direction: ControlWAL → substrate subjects CLUSTER.md:1283-1284; "Commit resolver becomes substrate consumer" :7628-7637. Counter-decision: "The resolver remains a single goroutine owned by `SessionVFS`..." SINGLE_PANE.md:86-93.

## D. AGENT-FACING API

- Unified workspace verbs (landed, `core/versioning/workspace_verbs.go`) PIPELINE_SKILL_REFACTOR.md:1011-1020: `workspace_read(op ∈ {read, glob, grep, inspect, summarize, diff, list_changes}, scope ∈ {pipeline, global}, …)`; `workspace_write(op ∈ {write, edit, delete, mkdir}, scope, basis, …)`; `prepare_write_context(scope ∈ {pipeline, global}, path, pipeline_id?)`; replaced 12 per-agent wrappers :739-743; "Inspector-pipeline stays read-only" :1026-1028; "−9 per agent × 6 agents = −54 registrations" :1037.
- Audit replica skills PGV:490-497: `workspace_read`/`workspace_write` scoped to `ReplicaVFS_N`; `merges_after(seq)` PGV:287; `emit_audit_decision(decision, summary, concerns)` PGV:329-334. `handoff_to_ot` is "the sole handoff verb" PGV:479.
- Error semantics (`docs/bugs/2026-06-14-workspace-read-wrapped-not-found-hard-error.md`): "`workspace_read` must **never** hard-error because a file/path is missing — absence returns structured missing metadata (read) or empty results (glob/grep)" :4-6; `ErrFileNotFound = fmt.Errorf("file not found: %w", fs.ErrNotExist)` at `core/versioning/vfs.go:22` :18-19; fix `isWorkspaceFileNotFound(err) = errors.Is(err, os.ErrNotExist)` :43-48; `ErrNoActiveSessionVFS` and `ErrVFSNotFound` "remain hard errors" :50-53; a hard error cascades "LLM retries → consecutive tool errors → `AbortToolLoop`" :23-25. Replica errors: `ErrReplicaVFSSealed` PGV:142; `ErrControlMarkerNotDurable` PGV:252.
- Claims integration CLAIMS.md:4632-4655: pre-write scope check; every write emits `update_claim_progress` artifact `kind=diff`; "scope check ≤ 50µs per write (cached)".
- Chokepoint: "`versioning.FileAccess` (Read/Write/Delete) — Every workspace file operation" FABRIC.md:740; capability tokens `ReadOnlyFileAccess` SI:34-37.
- Legacy versioning skills ARCHITECTURE.md:25718-25776 (`versioned_read_file(path, version?)`, `versioned_write_file(path, content, message?)`, `versioned_file_history(path, limit=10)`, `versioned_file_diff(path, old_version, new_version?)`, `versioned_rollback(path, version)`).
- Tool VFS skills: `provision_tool_dependency`, `attach_tool_to_view`, `query_view` TV:1103.

## E. CONCURRENCY

- ReplicaVFS: one mutex shared by inspector and tester PGV:137, :216; "The terminal-flag check, the ControlWAL append, and the in-memory mutation are atomic under the ReplicaVFS mutex" PGV:145.
- OT: "the same OT serializer — the one MergePipe already runs on a single goroutine for authoritative ordering. No parallel conflict algorithms, no split brain" PGV:161. Resolver single goroutine PGV:186. Merge callbacks synchronous PGV:118. Retention refcount + water-line fixpoint PGV:392-394.
- PureVFS/pods: "Every hot subsystem (`GoroutineBudget`, `ResourceQuota`, `ContainerRegistry`, `VolumeManager`, `ChunkStore`) has one mutex protecting everything" SI:118-119; proposed `atomic.Pointer[immutable.Map]` RCU + single-writer goroutine SI:121-123; "unmount-before-pause race in `core/container/pod/volume_manager.go` and the 'never tear down before commit' rule are both symptoms of an undefined ordering protocol" SI:132-134; proposed `Seal(gen_n)`/`ErrStaleGeneration` SI:136-138.
- CVS/PipelineVFS: `mu sync.RWMutex` + per-file `fileLocks` ARCHITECTURE.md:20818-20819.
- Tool VFS: 16-shard BlobStore TV:1021; CRDT lockfile TV:765-772.
- Known races/bugs with root causes: (1) wrap-unaware not-found check (`os.IsNotExist` does not unwrap `%w`); (2) CommitWAL three-leg defect + latent open-segment GC bug; (3) slow `workspace_read` globs "(8–10s each)" starved the claim-inactivity watchdog because liveness "fires **only at tool completion**" `docs/bugs/2026-06-15-carry-forward-wedge-three-causes.md:33-38`; (4) Guardian `RequestSerializer` head-of-line blocking `docs/bugs/2026-06-20-late-guardian-gate-on-academic-webfetch.md:53-58`; (5) pipeline-worker identity `docs/bugs/2026-06-17-pipeline-worker-identity-unresolved-and-duplicate-render.md:108-110`.

## F. PERFORMANCE NUMBERS

- PGV: dispatch ceiling = GOMAXPROCS × 2 PGV:407; read amplification "≈ O(depth) with tiny constants" PGV:559; per-audit O(diff size)/O(chain depth) PGV:555.
- TOOL_VFS: BLAKE3 "~5× faster than SHA-256" TV:134; "peak scratch size for a typical build (e.g. CPython) is ~2GB" TV:626; tiers Hot 25% sysmem/nanoseconds, Warm 35%/microseconds, Cold unbounded/seconds TV:791-795; TinyLFU 4-bit sketch TV:805; zstd dict ~64KB, "5-8× with a Python-trained dict vs ~3×" TV:811; pytest versions "share ~95% of their blobs" TV:772; cost "O(unique_bytes), not O(pipelines × packages)" TV:15. RESOLVER.md:136-140 reflink venv "materializes in milliseconds".
- SYSTEM_IMPROVEMENTS: cool tier "5–10× density" SI:49; 256-way stripe SI:62; "Cold pods cost ~bytes of header" SI:91.
- CLAIMS.md: scope check "≤ 50µs per write (cached)" :4655.
- Bug docs: 5,499 segments, avg 78 bytes, ~21 MB commit-wal:4; `workspace_read` globs 8–10 s carry-forward:33.
- ARCHITECTURE.md: checkpoint every 5 s :6822; budgets query cache 500MB, staging 1GB, agent context 500MB, WAL 200MB, ceiling 0.8, eviction 0.7/0.9/0.95 :12656-12683; sandbox defaults CPU 80%, 1024 MB, 256 files, 32 procs, 30 s :20504-20508.
- PERFORMANCE.md: WAL 64MB segments, 100 ms sync interval :399-402; hot cache 32MB :421-427; adaptive channel 16/4096/10000 :340-343.
- DB.md targets (p99) :1996-2004: GetNode session <500 ns, global <100 ns, FindNode <1 µs, AddNode <10 µs, Checkout <1 µs. DB_TODO.md: "Read @ 0.24ns/op, AppendParallel @ 107μs/op (lock-free COW)" :59.

## G. DESIGN RATIONALE / TODOs / KNOWN LIMITS

- Rejected alternatives PGV:59-83 (persistent rejected-overlay tier "memory grows linearly with rejection depth"; MVCC snapshot handles; eager materialization "wasteful"; split per-role overlays; bus/orchestrator dispatch). Non-goals PGV:648-653. Open questions PGV:657-661.
- Tool VFS principles TV:13-20 (strict no-disk, install once, recipes are content, hermetic, Guardian owns policy, fabric observability, disk fallback); risk table TV:1153-1176; out of scope TV:1180-1187 (multi-node sharing, manifest-level GC).
- SYSTEM_IMPROVEMENTS proposals: compressed in-RAM cool tier :48-49; eviction by reconstruction cost :50-52; epoch-based snapshot GC :53-56; inline small files "ZFS-style" :57-58; anonymous mmap arenas with `MADV_FREE`/`DiscardVirtualMemory` :59-61; sharded ChunkStore :62-64; global content-addressed read plane and "Capability-gated handoff = pointer transfer" :76-81; speculative forks via shared CoW snapshot :108-114; fenced two-phase handoff :136-146; time-travel debugging via the journal :164-173.
- WAL_TODO decisions :142-149 (no hardcoded thresholds; WAL first then checkpoint; commit markers; signal-aware).
- SINGLE_PANE decisions: D2 resolver keeps owner :86-93; D3 `CommittedBaseline` port :95-103; unchanged: "PureVFS in RAM with no spill; the OT serializer" :323.
- Claims conversion of VFS provisioners CLAIMS_AND_INFRASTRUCTURE.md:4583-4618: services emit claims for "provision, attach, detach, snapshot, promote, merge, and rollback"; artifacts must include "mount reference, scope boundary, capacity budget, base version, CoW layer chain, snapshot hash, merge preview, conflict set, and resulting version".
- MCP rules MCP.md:989-996 (resolved workspace view, `SessionID` + `CapabilityScope`).
