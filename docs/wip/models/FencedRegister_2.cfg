\* Smaller TLC configuration: three holders, two hosts, two epochs (one takeover
\* plus a resumed stale owner), two records per epoch.
SPECIFICATION Spec
CONSTANTS
  Acceptors = {a1, a2, a3}
  Hosts = {h1, h2}
  Values = {v1}
  MaxEpoch = 2
  MaxSeq = 2
INVARIANTS
  TypeOK
  TotalOrder
  Continuity
  StaleNeverCommits
  ReadSafety
