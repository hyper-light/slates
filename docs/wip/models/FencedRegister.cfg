\* TLC configuration for FencedRegister: three holders (f = 1), two hosts,
\* three epochs, two records per epoch, two payload values.
SPECIFICATION Spec
CONSTANTS
  Acceptors = {a1, a2, a3}
  Hosts = {h1, h2}
  Values = {v1}
  MaxEpoch = 3
  MaxSeq = 2
INVARIANTS
  TypeOK
  TotalOrder
  Continuity
  StaleNeverCommits
  ReadSafety
