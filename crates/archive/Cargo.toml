[package]
name = "slates-archive"
version.workspace = true
authors.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
readme.workspace = true
description = "The archive format (D-17, §4.16 archive, research/compression-archive-dedup.md §2.6): a single-pass, streamable, content-addressed, self-verifying byte stream of a snapshot — header, chunk records, manifest, seek table and trailer, each chunk BLAKE3-verified before use (Phase 7). Never written to disk by slates."

[dependencies]
blake3 = { workspace = true }
lz4_flex = { workspace = true }
# zstd is the C-backed ratio codec (D-17). It is a default feature so native builds use it, but the
# cross-compile lint gate runs `--no-default-features` and so does not need a C cross-toolchain for
# the Windows/Linux targets; a `ruzstd` decode-only fallback for those builds is owed (GAPS §8g).
zstd = { workspace = true, optional = true }

[features]
default = ["zstd"]
# The zstd codec (encode and decode); off on targets without a C toolchain.
zstd = ["dep:zstd"]

[dev-dependencies]
proptest = { workspace = true }

[lints]
workspace = true
